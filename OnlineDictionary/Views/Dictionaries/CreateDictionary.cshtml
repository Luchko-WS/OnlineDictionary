<div class="modal-header">
    <button type="button" class="close" ng-click=""><span aria-hidden="true">&times;</span></button>
    <h4 class="modal-title" id="modalTitle">Create dictionaty</h4>
</div>

<div class="modal-body" style="padding-left: 50px; padding-right: 50px">
    <div class="form-horizontal">
        <div class="form-group">
            <label>Name</label>
            <input class="form-control" ng-model="vm.dictionary.name" />

            <label>Decription</label>
            <input class="form-control" ng-model="vm.dictionary.description" />

            <label>From lamguage</label>
            <input class="form-control" ng-model="vm.dictionary.fromLanguage" />

            <label>To language</label>
            <input class="form-control" ng-model="vm.dictionary.toLanguage" />

            <label>Is public</label>
            <input type="checkbox" ng-model="vm.dictionary.isPublic" />
        </div>
    </div>
    <button class="btn" ng-click="vm.createDictionary()">Save</button>
    <button class="btn">Cancel</button>
</div>
